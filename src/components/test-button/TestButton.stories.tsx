import React from 'react';

import { TestButton } from './TestButton';

export default {
  component: TestButton,
  title: 'Components/Button',
};

const Template = (args) => <TestButton />;

export const Default = Template.bind({});
