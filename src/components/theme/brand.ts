// ─────────────────────────────────────────────────────────────────────────────
// BRAND CONFIG — edit here to switch the site's look for either theme.
// Both objects share the same keys so swapping is straightforward.
// ─────────────────────────────────────────────────────────────────────────────

export const lightBrand = {
  base: '#81A6C6', // hero / header / footer background
  accent: '#3B6E9A', // links, accents, nav hover/active on white
};

export const darkBrand = {
  base: '#1f1f1f', // hero / header / footer background
  accent: '#81A6C6', // links, hovers, borders, buttons
};
